services:
    nginx:
        image: nginx:latest
        ports:
            - "8080:80"
        volumes:
            - ./devops/nginx/conf.d:/etc/nginx/conf.d
            - ./wordpress:/var/www/html
        depends_on:
            - php
        networks:
            - app-network

    php:
        build:
            context: .
            dockerfile: devops/php/Dockerfile
        volumes:
            - ./wordpress:/var/www/html
        env_file:
            - .env
        depends_on:
            - mysql
        networks:
            - app-network

    mysql:
        image: mysql:latest
        env_file:
            - .env
        volumes:
            - mysql-data:/var/lib/mysql
        networks:
            - app-network

    phpmyadmin:
        image: phpmyadmin:latest
        ports:
            - "8081:80"
        env_file:
            - .env
        depends_on:
            - mysql
        networks:
            - app-network

    node:
        image: node:latest
        volumes:
            - ./wordpress:/usr/src/app
        working_dir: /usr/src/app
        command: npm start
        ports:
            - "3000:3000"
        networks:
            - app-network

volumes:
    mysql-data:

networks:
    app-network:
        driver: bridge